---
title: 'Introduction'
description: 'Welcome to LND Boilerplate - a modern web development platform with comprehensive examples.'
---

# Introduction

Welcome to LND Boilerplate, a comprehensive and modern web development platform built with Next.js 14, TypeScript, and Tailwind CSS. This page demonstrates all possible MDX elements and styling.

## Typography Examples

### Headings

# H1 Heading

## H2 Heading

### H3 Heading

#### H4 Heading

##### H5 Heading

###### H6 Heading

### Text Formatting

This is **bold text** and this is _italic text_. You can also use **_bold and italic_** together.

Here's some `inline code` and here's a [link to Google](https://google.com).

~~Strikethrough text~~ and <mark>highlighted text</mark>.

### Lists

#### Unordered List

- First item
- Second item
  - Nested item
  - Another nested item
- Third item

#### Ordered List

1. First step
2. Second step
   1. Sub-step A
   2. Sub-step B
3. Third step

#### Task List

- [x] Completed task
- [ ] Incomplete task
- [x] Another completed task

## Code Examples

### Inline Code

Use `console.log()` to output messages to the browser console.

### Code Blocks

```javascript
// JavaScript example
function greetUser(name) {
  console.log(`Hello, ${name}!`);
  return `Welcome to LND Boilerplate, ${name}`;
}

const user = 'Developer';
const message = greetUser(user);
```

```typescript
// TypeScript example
interface User {
  id: number;
  name: string;
  email: string;
  isActive: boolean;
}

const createUser = (userData: Omit<User, 'id'>): User => {
  return {
    id: Math.random(),
    ...userData,
  };
};
```

```css
/* CSS example */
.button {
  background-color: #3b82f6;
  color: white;
  padding: 0.5rem 1rem;
  border-radius: 0.375rem;
  border: none;
  cursor: pointer;
  transition: background-color 0.2s;
}

.button:hover {
  background-color: #2563eb;
}
```

```bash
# Bash commands
npm install
npm run dev
npm run build
npm run test
```

## Tables

| Feature          | Description               | Status      |
| ---------------- | ------------------------- | ----------- |
| Next.js 14       | Latest React framework    | ✅ Complete |
| TypeScript       | Type-safe JavaScript      | ✅ Complete |
| Tailwind CSS     | Utility-first CSS         | ✅ Complete |
| MDX Support      | Rich content creation     | ✅ Complete |
| SEO Optimization | Built-in SEO utilities    | ✅ Complete |
| Performance      | Core Web Vitals optimized | ✅ Complete |

## Blockquotes

> This is a simple blockquote. It can contain multiple lines of text and will be styled appropriately.

> **Important Note**: This is a blockquote with bold text. It's perfect for highlighting important information or quotes from other sources.

> ### Blockquote with Heading
>
> This blockquote contains a heading and multiple paragraphs. It's useful for callouts and important notices.

## Horizontal Rules

---

## Images

![LND Boilerplate Logo](https://via.placeholder.com/400x200/3b82f6/ffffff?text=LND+Boilerplate)

## Definition Lists

Term 1
: Definition for term 1

Term 2  
: Definition for term 2
: Another definition for term 2

## Abbreviations

The HTML specification is maintained by the W3C.

_[HTML]: HyperText Markup Language
_[W3C]: World Wide Web Consortium

## Footnotes

Here's a sentence with a footnote[^1]. And here's another one[^2].

[^1]: This is the first footnote.

[^2]: This is the second footnote with more details.

## Math Examples

Inline math: E = mc²

Block math:

```
∫[-∞,∞] e^(-x²) dx = √π
```

## Callouts/Admonitions

<div className="callout-info">
  > **ℹ️ Info**: This is an info callout. Use it to provide helpful information
  to users.
</div>

<div className="callout-warning">
  > **⚠️ Warning**: This is a warning callout. Use it to alert users about
  potential issues.
</div>

<div className="callout-danger">
  > **🚨 Danger**: This is a danger callout. Use it for critical warnings or
  error messages.
</div>

<div className="callout-tip">
  > **💡 Tip**: This is a tip callout. Use it to provide helpful tips and best
  practices.
</div>

## Details/Summary

<details>
<summary>Click to expand</summary>

This content is hidden by default and can be expanded by clicking the summary. It's perfect for FAQ sections or optional information.

```javascript
// Code can be inside details too
const example = 'This works!';
console.log(example);
```

</details>

## HTML Elements

<div
  style={{
    background: '#f3f4f6',
    padding: '1rem',
    borderRadius: '0.5rem',
    margin: '1rem 0',
  }}
>
  <h4>Custom HTML Block</h4>
  <p>
    This is a custom HTML div with inline styles. It demonstrates that raw HTML
    works in MDX.
  </p>
</div>

## Complex Example

Here's a comprehensive example combining multiple elements:

### React Component Example

```jsx
import React, { useState } from 'react';

const Counter = () => {
  const [count, setCount] = useState(0);

  return (
    <div className="p-4 border rounded">
      <p>Count: {count}</p>
      <button
        onClick={() => setCount(count + 1)}
        className="px-4 py-2 bg-blue-500 text-white rounded"
      >
        Increment
      </button>
    </div>
  );
};
```

### API Response Example

```json
{
  "status": "success",
  "data": {
    "users": [
      {
        "id": 1,
        "name": "John Doe",
        "email": "john@example.com",
        "isActive": true
      },
      {
        "id": 2,
        "name": "Jane Smith",
        "email": "jane@example.com",
        "isActive": false
      }
    ],
    "pagination": {
      "page": 1,
      "limit": 10,
      "total": 2
    }
  }
}
```

## Conclusion

This page demonstrates all the possible MDX elements and styling that should work in your documentation system. If you see all these elements styled correctly, your MDX setup is working perfectly!

> **Next Steps**: Check out our [Installation Guide](/docs/installation) to get started with LND Boilerplate.
