import { 
  getDefaultLocaleClient as getDefaultLocale, 
  getSupportedLocalesClient as getSupportedLocales, 
  isDefaultLocaleClient as isDefaultLocale,
  getLocaleDisplayName,
  getLocaleFlag
} from '@lnd/utils/config'

// Re-export the locale utilities
export { 
  getDefaultLocale, 
  getSupportedLocales, 
  isDefaultLocale,
  getLocaleDisplayName,
  getLocaleFlag
}
