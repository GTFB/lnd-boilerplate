// Export base styles
// Note: CSS files cannot be exported as modules, they should be imported directly

// Export design system styles
// Note: CSS files cannot be exported as modules, they should be imported directly
